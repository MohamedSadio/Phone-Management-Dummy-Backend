{
  "name": "shope-smart-api",
  "version": "1.0.0",
  "description": "JSON Server API for Shope Smart",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}